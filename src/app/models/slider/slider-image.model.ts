export class SliderImage{
  public imageName: string;
  public isShown: boolean;

  constructor(imageName: string, isShown: boolean) {
    this.imageName = imageName;
    this.isShown = isShown;
  }
}
